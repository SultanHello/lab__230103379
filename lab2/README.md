# Lab 2

Pi integration: serial baseline, unsynchronized race, critical section, and parallel reduction. Run `javac Lab2Pi.java && java Lab2Pi 100000000 3 > lab2-results.csv`. Summary CSV is derived from raw trials.

Files in this folder: Java source, raw CSV measurements, summary CSV where available, and this lab section exported as a PDF.
